def build_update_audio_order(update_audio_order):
    def update_audio_order(id, audiolink, charsnames) -> bool:
        return update_audio_order(id, audiolink, charsnames)
    return update_audio_order
